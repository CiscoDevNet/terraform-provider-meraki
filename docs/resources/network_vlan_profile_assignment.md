---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "meraki_network_vlan_profile_assignment Resource - terraform-provider-meraki"
subcategory: "Networks"
description: |-
  This resource manages the assignment of a VLAN profile to specific devices and switch stacks within a network.
---

# meraki_network_vlan_profile_assignment (Resource)

This resource manages the assignment of a VLAN profile to specific devices and switch stacks within a network.

## Example Usage

```terraform
resource "meraki_network_vlan_profile_assignment" "example" {
  network_id         = "L_123456"
  vlan_profile_iname = "Profile1"
  serials            = ["Q234-ABCD-5678"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `network_id` (String) Network ID
- `vlan_profile_iname` (String) IName of the VLAN Profile

### Optional

- `serials` (Set of String) Array of Device Serials
- `stack_ids` (Set of String) Array of Switch Stack IDs

### Read-Only

- `id` (String) The id of the object

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = meraki_network_vlan_profile_assignment.example
  identity = {
    "network_id" : "<network_id>"
    "vlan_profile_iname" : "<vlan_profile_iname>"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `network_id` (String) Network ID
- `vlan_profile_iname` (String) IName of the VLAN Profile

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = meraki_network_vlan_profile_assignment.example
  id = "<network_id>,<vlan_profile_iname>"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import meraki_network_vlan_profile_assignment.example "<network_id>,<vlan_profile_iname>"
```
