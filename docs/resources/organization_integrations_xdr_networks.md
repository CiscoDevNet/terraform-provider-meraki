---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "meraki_organization_integrations_xdr_networks Resource - terraform-provider-meraki"
subcategory: "Organizations"
description: |-
  This resource can manage the Organization Integrations XDR Networks configuration.
---

# meraki_organization_integrations_xdr_networks (Resource)

This resource can manage the `Organization Integrations XDR Networks` configuration.

## Example Usage

```terraform
resource "meraki_organization_integrations_xdr_networks" "example" {
  organization_id = "123456"
  networks = [
    {
      network_id    = "N_1234567"
      product_types = ["appliance"]
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `networks` (Attributes List) List containing the network ID and the product type to enable XDR on (see [below for nested schema](#nestedatt--networks))
- `organization_id` (String) Organization ID

### Read-Only

- `id` (String) The id of the object

<a id="nestedatt--networks"></a>
### Nested Schema for `networks`

Required:

- `network_id` (String) Network ID
- `product_types` (List of String) List of products for which to enable XDR

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = meraki_organization_integrations_xdr_networks.example
  identity = {
    "organization_id" : "<organization_id>"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `organization_id` (String) Organization ID

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = meraki_organization_integrations_xdr_networks.example
  id = "<organization_id>"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import meraki_organization_integrations_xdr_networks.example "<organization_id>"
```
