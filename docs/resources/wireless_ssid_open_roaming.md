---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "meraki_wireless_ssid_open_roaming Resource - terraform-provider-meraki"
subcategory: "Wireless"
description: |-
  This resource can manage the Wireless SSID Open Roaming configuration.
---

# meraki_wireless_ssid_open_roaming (Resource)

This resource can manage the `Wireless SSID Open Roaming` configuration.

## Example Usage

```terraform
resource "meraki_wireless_ssid_open_roaming" "example" {
  network_id = "L_123456"
  number     = "0"
  enabled    = false
  tenant_id  = "12345"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `network_id` (String) Network ID
- `number` (String) Wireless SSID number

### Optional

- `enabled` (Boolean) If true, OpenRoaming is enabled on this SSID.
- `tenant_id` (String) The OpenRoaming DNA Spaces tenant ID.

### Read-Only

- `id` (String) The id of the object

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = meraki_wireless_ssid_open_roaming.example
  identity = {
    "network_id" : "<network_id>"
    "number" : "<number>"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `network_id` (String) Network ID
- `number` (String) Wireless SSID number

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = meraki_wireless_ssid_open_roaming.example
  id = "<network_id>,<number>"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import meraki_wireless_ssid_open_roaming.example "<network_id>,<number>"
```
